# Claude Code artefacts

- `session-2026-09-13-e2e-suite.md` — full transcript of the Claude Code session that built the Playwright suite, the fixes (`docs/fixes/`) and the workflow validation.
- `session-2026-09-13-e2e-suite.jsonl` — the raw Claude Code transcript.
- `memory/` — Claude Code project memory notes used across sessions.
